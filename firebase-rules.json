{
  "rules": {
    "games": {
      "$gameId": {
        ".read": "auth != null",
        ".write": "auth != null && (
          !data.exists() || 
          data.child('hostPlayer').val() == auth.uid || 
          data.child('guestPlayer').val() == auth.uid ||
          (!data.child('guestPlayer').exists() || data.child('guestPlayer').val() == null)
        )"
      }
    },
    "waitList": {
      "$gameId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}

